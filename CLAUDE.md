# プロジェクト概要

オリジナルのホームページ(HP)を作成するプロジェクト。AIで作成したBGMをHP上に載せて聴けるようにする。

## 現状

- プロジェクトは初期段階。まだコード資産なし。
- 技術スタック(静的HTML/CSS/JSにするか、Next.js等のフレームワークを使うか)は未定。会話しながら決めていく。

## BGMの扱い方針

- AIで作成したBGM(mp3等)はリポジトリ内に直接配置する予定。
- 目安として、音声ファイルは容量が大きくなりやすいので、ファイル数・サイズが増えてきたら配置場所やGit管理方法(LFS等)を見直す。

## 公開方法

- Cloudflare(Cloudflare Pages)でHPを公開する。

## 今後決めること

- 技術スタック(静的サイト or フレームワーク)
- ページ構成・デザイン方針
- BGMの再生方法(HTML5 audioタグ、プレイリストUI等)
