# Claude Code コマンド一覧・リファレンス

> Claude Code（CLIツール）で使えるコマンド・機能をカテゴリ別にまとめた個人用リファレンスです。
> 作成日: 2026-08-30 / 出典: 公式ドキュメント (code.claude.com/docs) をもとに調査

---

## 目次

1. [スラッシュコマンド（対話セッション中）](#1-スラッシュコマンド対話セッション中)
2. [ターミナルから起動する際のコマンド・フラグ](#2-ターミナルから起動する際のコマンドフラグ)
3. [キーボードショートカット](#3-キーボードショートカット)
4. [特殊機能の概要](#4-特殊機能の概要)
5. [補足メモ](#5-補足メモ)

---

## 1. スラッシュコマンド（対話セッション中）

セッション中に `/` を入力すると候補が表示される。

### セッション・作業フロー管理

| コマンド | 説明 |
|---|---|
| `/clear [name]` | 会話をリセットし、空のコンテキストで新規開始 |
| `/resume` | 過去の会話に戻る |
| `/branch [name]` | 現在の会話から分岐（別方向を試したい時） |
| `/fork [prompt]` | 現在の会話をコピーしてバックグラウンドの新セッションへ |
| `/background [prompt]` | 現在のセッションをバックグラウンドエージェントとして切り離す |
| `/tasks` | バックグラウンド処理・サブエージェントの一覧を表示 |
| `/exit` | Claude Codeを終了 |

### モデル・パフォーマンス

| コマンド | 説明 |
|---|---|
| `/model [model]` | 使用するAIモデルを切り替え |
| `/effort [level]` | 推論の労力レベルを設定（low / medium / high / max など） |
| `/fast [on\|off]` | 高速モードの切り替え |
| `/advisor [model\|off]` | アドバイザー機能の有効/無効 |

### コードレビュー・分析

| コマンド | 説明 |
|---|---|
| `/code-review [level] [--fix] [--comment] [対象]` | 差分やPRのバグ・改善点をレビュー |
| `/security-review` | 差分のセキュリティ脆弱性をチェック |
| `/diff` | 未コミットの変更を対話的に表示 |
| `/simplify` | 変更したコードの簡略化・リファクタ提案 |
| `/batch <指示>` | コードベース全体への大規模な並列変更を指揮 |

### コンテキスト・ヘルプ

| コマンド | 説明 |
|---|---|
| `/context [all]` | 現在のコンテキスト使用状況を可視化 |
| `/compact [指示]` | 会話を要約してコンテキストを圧縮 |
| `/help` | ヘルプ・利用可能コマンド一覧を表示 |
| `/usage` | トークン・コスト使用量を表示 |

### プロジェクト設定

| コマンド | 説明 |
|---|---|
| `/permissions` | ツールの許可/確認/拒否ルールを管理 |
| `/add-dir <path>` | ファイルアクセス用の作業ディレクトリを追加 |
| `/cd <path>` | セッションの作業ディレクトリを変更 |
| `/mcp [reconnect\|enable\|disable]` | MCPサーバー接続を管理 |
| `/config [key=value]` | テーマ・モデルなどの設定を変更 |

### メモリ・ドキュメント

| コマンド | 説明 |
|---|---|
| `/init` | CLAUDE.mdを生成してプロジェクトを初期化 |
| `/memory` | CLAUDE.mdや自動メモリ機能を編集 |

### ユーティリティ

| コマンド | 説明 |
|---|---|
| `/copy [N]` | 直前のClaudeの返答をクリップボードにコピー |
| `/export [filename]` | 会話をテキストとしてエクスポート |
| `/login` / `/logout` | ログイン/ログアウト |
| `/theme [color\|default]` | セッションの配色テーマを設定 |
| `/terminal-setup` | Shift+Enterなどのキー入力用にターミナルを設定 |
| `/keybindings` | キーボードショートカット設定ファイルを開く |
| `/bug [report]` | セッション情報付きでバグを報告 |
| `/feedback [report]` | 製品フィードバックを送信 |
| `/doctor` | セットアップの診断・トラブルシューティング |

### この環境で使えるスキル（`/`で呼び出し）

| コマンド | 説明 |
|---|---|
| `/dataviz [依頼内容]` | グラフ・ダッシュボード作成のデザイン指針 |
| `/design [依頼内容]` | デザインキャンバス（UIモックアップ等）を作成 |
| `/loop [間隔] [プロンプト]` | プロンプトを一定間隔で繰り返し実行 |
| `/run` | このプロジェクトのアプリを起動・確認 |
| `/security-review` | セキュリティレビュー |
| `/update-config` | settings.json（権限・フック等）の設定変更 |
| `/keybindings-help` | キーバインドのカスタマイズ支援 |
| `/claude-api` | Claude API / Anthropic SDKのリファレンス |

---

## 2. ターミナルから起動する際のコマンド・フラグ

### 基本コマンド

| コマンド | 用途 | 例 |
|---|---|---|
| `claude` | 対話セッションを開始 | `claude` |
| `claude "質問"` | 初期プロンプト付きで開始 | `claude "このプロジェクトを説明して"` |
| `claude -p "質問"` | 1回だけ質問して終了（非対話） | `claude -p "この関数を説明して"` |
| `cat file \| claude -p "質問"` | パイプで渡した内容を処理 | `cat log.txt \| claude -p "分析して"` |
| `claude -c` | 直近の会話を継続 | `claude -c` |
| `claude -r "<session>"` | セッションIDや名前で再開 | `claude -r "auth-refactor"` |
| `claude --version` | バージョン確認 | `claude --version` |
| `claude doctor` | 診断情報を表示 | `claude doctor` |
| `claude update` | 最新版に更新 | `claude update` |
| `claude mcp` | MCPサーバーの設定 | `claude mcp` |
| `claude plugin` | プラグイン管理 | `claude plugin install <name>` |

### 権限・セキュリティ関連フラグ

| フラグ | 用途 | 例 |
|---|---|---|
| `--permission-mode` | 起動時の権限モードを指定 | `claude --permission-mode plan` |
| `--dangerously-skip-permissions` | 権限確認をすべてスキップ（注意） | — |
| `--allowedTools` | 確認なしで実行してよいツール | `--allowedTools "Bash(git *)" "Read"` |
| `--disallowedTools` | 拒否するツール | `--disallowedTools "Bash(rm *)"` |

### モデル・出力関連フラグ

| フラグ | 用途 | 例 |
|---|---|---|
| `--model` | 使用モデルを指定 | `claude --model opus` |
| `--effort` | 推論の労力レベル | `claude --effort high` |
| `--output-format` | 出力形式指定 | `--output-format json` |
| `--verbose` | 詳細出力 | `claude -p --verbose "質問"` |

### ファイル・ディレクトリ関連フラグ

| フラグ | 用途 | 例 |
|---|---|---|
| `--add-dir` | 追加の作業ディレクトリ | `claude --add-dir ../lib` |
| `--mcp-config` | MCPサーバー設定をJSONから読込 | `claude --mcp-config ./mcp.json` |
| `--settings` | 設定ファイルを指定して読込 | `claude --settings ./settings.json` |

> ⚠️ `--dangerously-skip-permissions` は全ての安全確認を無効化するオプションです。信頼できる環境以外では使用しないこと。

---

## 3. キーボードショートカット

### 基本操作

| ショートカット | 説明 |
|---|---|
| `Ctrl+C` | 入力を中断・クリア（2回押すと終了） |
| `Ctrl+D` | セッション終了（2回押す） |
| `Ctrl+O` | ツール詳細を含むトランスクリプト表示を切替 |
| `Ctrl+R` | コマンド履歴を逆順検索 |
| `Ctrl+V` | クリップボードの画像を貼り付け |
| `Ctrl+L` | 画面再描画（表示崩れの修正） |
| `Esc` | Claudeの処理を中断／ダイアログを閉じる |
| `Esc` を2回 | 入力下書きをクリア／巻き戻しメニューを開く |
| `Shift+Tab` | 権限モードを順に切り替え |
| `Tab` | 自動補完候補を確定 |
| `↑` / `↓` | コマンド履歴を移動 |

### 入力補助（`/`, `!`, `@` など）

| 記号 | 説明 |
|---|---|
| `/` を先頭に入力 | コマンド・スキルメニューを表示 |
| `!` を先頭に入力 | シェルモード（コマンドを直接実行） |
| `@` | ファイルパスの補完・メンション |
| `?`（空の入力で） | ショートカットヘルプの表示切替 |

### 改行（複数行入力）

| 方法 | 備考 |
|---|---|
| `\` + `Enter` | どのターミナルでも動作 |
| `Shift+Enter` | 多くのターミナルで対応（`/terminal-setup`で設定可） |
| `Ctrl+J` | どのターミナルでも動作 |

### テキスト編集

| ショートカット | 説明 |
|---|---|
| `Ctrl+A` / `Ctrl+E` | 行頭／行末に移動 |
| `Ctrl+K` | カーソルから行末まで削除 |
| `Ctrl+U` | カーソルから行頭まで削除 |
| `Ctrl+W` | 直前の単語を削除 |

---

## 4. 特殊機能の概要

| 機能 | 概要 |
|---|---|
| **CLAUDE.md** | プロジェクトの規約・ビルド手順などを毎セッション読み込ませる永続コンテキストファイル |
| **スキル (Skills)** | `/名前`で呼び出せる再利用可能なワークフロー・知識パッケージ |
| **サブエージェント** | 独立したコンテキストを持つワーカー。並列作業やコンテキスト分離に利用 |
| **MCPサーバー** | 外部サービス・DB・APIとClaudeを接続する仕組み |
| **フック (Hooks)** | ツール実行前後などのタイミングでスクリプトを自動実行する仕組み |
| **プラグイン** | スキル・フック・サブエージェント・MCPをまとめて配布できるパッケージ |
| **プランモード** | 変更を実行する前に計画を提示し、承認を得てから実行するモード（`Shift+Tab`で切替） |
| **Artifacts** | セッションの成果物を非公開のWebページとして公開・共有できる機能 |
| **自動メモリ** | ユーザーの好みや過去のやり取りを記憶し、以後のセッションに活用する仕組み |

---

## 5. 補足メモ

- 権限モードには **手動確認 / 自動 / プラン / 編集自動承認 / バイパス** の5種類があり、`Shift+Tab` で切替可能。
- OSによって使えるツールが異なる（例: BashツールはmacOS/Linux/WSL、Windowsでは主にPowerShellツール）。
- 最新の正確な情報は公式ドキュメントを参照:
  - CLIリファレンス: https://code.claude.com/docs/en/cli-reference.md
  - コマンドリファレンス: https://code.claude.com/docs/en/commands.md
- 本ファイルは調査時点（2026-08-30）の情報。Claude Codeはアップデートが頻繁なため、コマンド名や挙動が変わっている可能性がある。迷ったら `/help` または `claude --help` で最新の一覧を確認すること。
